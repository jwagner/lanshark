include: HEADER
-
*External Dependencies
-
python >= 2.5 (does not work with 2.4!)
pygtk >= 2.10 (only for the GUI)
-
*Installation
-
As simple as that:
# python setup.py install
-
*Use without installation
-
You can also use lanshark without installing it
$ ./src/lansharkgui
-
